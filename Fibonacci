pipeline {
    agent any

    stages {
        stage('Build Docker image') {
            steps {
                script {
                    docker.withRegistry('https://registry.hub.docker.com', 'Docker_Login') {
                        def dockerImage = docker.build('fibonacci-image', '.')
                        dockerImage.push()
                    }
                }
            }
        }

        stage('Cleanup') {
            steps {
                cleanWs()
            }
        }
    }
}
